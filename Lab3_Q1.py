X = float(input('enter the length of 1st parallel side in m: '))
Y = float(input('enter the length of 2nd parallel side in m: '))
H = float(input('enter the height in m: '))
a = 0.5*(X+Y)*H
print('Area of trapezoid is ',a,'sq m.')
